import styled from "styled-components";

const Featured = () => {
  return <Wrapper>featured</Wrapper>;
};

const Wrapper = styled.section`
  height: 500px;
  background-color: var(--primary-100);
`;

export default Featured;
