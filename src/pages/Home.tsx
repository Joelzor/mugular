import { Hero, Featured, Services, ContactForm } from "../components";

const Home = () => {
  return (
    <main>
      <Hero />
      <Featured />
      <Services />
      <ContactForm />
    </main>
  );
};

export default Home;
